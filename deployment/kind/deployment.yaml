apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlops-flask-api
  labels:
    app: mlops-flask-api
    component: prediction-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mlops-flask-api
  template:
    metadata:
      labels:
        app: mlops-flask-api
        component: prediction-api
    spec:
      containers:
        - name: api
          image: mlops-flask-api:latest
          imagePullPolicy: Never
          env:
            - name: MODEL_PATH
              value: models/breast_cancer_classifier.joblib
          ports:
            - containerPort: 5000
              name: http
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
