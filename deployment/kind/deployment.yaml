apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlops-flask-api
  labels:
    app: mlops-flask-api
    component: prediction-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mlops-flask-api
  template:
    metadata:
      labels:
        app: mlops-flask-api
        component: prediction-api
    spec:
      containers:
        - name: api
          image: mlops-flask-api:latest
          imagePullPolicy: Never
          env:
            - name: MODEL_PATH
              value: models/wine_quality_classifier.joblib
          ports:
            - containerPort: 5000
              name: http
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 45
            periodSeconds: 5
            timeoutSeconds: 10
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 75
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 6
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
