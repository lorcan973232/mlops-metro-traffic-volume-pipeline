# Pinned dependencies make local runs, GitHub Actions, Docker builds, and the
# saved scikit-learn model artefact use a compatible environment. In particular,
# scikit-learn is pinned because joblib model files are not stable across all
# future scikit-learn versions.
Flask==3.1.3
gunicorn==22.0.0
joblib==1.5.3
numpy==1.26.4
openpyxl==3.1.5
pandas==2.2.2
pytest==9.0.3
PyYAML==6.0.2
requests==2.33.0
ruff==0.5.5
scikit-learn==1.5.1
shap==0.46.0
